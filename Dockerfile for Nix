# Use a base image from the official Nginx repository
FROM nginx:latest

# Copy the custom HTML file into the Nginx public directory.
# The `index.html` file is placed at `/usr/share/nginx/html/index.html`
# to be served as the default page.
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80 to the outside world, so the web server can be accessed.
EXPOSE 80
